package dev.slne.surf.teleporter.core.client.dialog

/**
 * How the input fields of the teleporter dialogs are laid out.
 */
object TeleporterInputTexts {
    /**
     * The width every input field of the teleporter dialogs uses.
     */
    const val INPUT_WIDTH = 400

    /**
     * The largest value an input field of the teleporter dialogs accepts.
     */
    const val INPUT_MAX_LENGTH = Int.MAX_VALUE
}
